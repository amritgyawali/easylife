node_modules
dist
web-build
.expo
coverage
package-lock.json
supabase/.branches
supabase/.temp
# No SQL parser plugin installed — SQL files are hand-formatted consistently instead.
*.sql

# Emitted verbatim by `npm run db:types`; reformatting it would make every
# regeneration show up as a diff.
src/types/database.ts

# Generated knowledge-graph output from the graphify skill.
graphify-out
